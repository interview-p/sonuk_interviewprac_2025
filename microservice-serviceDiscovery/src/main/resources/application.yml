server:
  port: ${PORT:9091}

eureka:
  instance:
    hostname: ${EUREKA_INSTANCE_HOSTNAME:eureka1}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://eureka2:9092/eureka/,http://eureka3:9093/eureka/}